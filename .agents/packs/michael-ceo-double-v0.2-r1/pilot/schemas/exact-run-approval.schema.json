{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "michael.ceo_double.shadow_exact_run_approval.v1",
  "type": "object",
  "required": ["contract_id", "contract_version", "approval_status", "approval_id", "run_id", "approved_at", "expires_at", "pack_baseline_commit", "execution_commit", "pilot_contract_sha256", "execution_bundle_sha256", "object_id", "object_label", "legal_entity_id", "organization_id", "scope_id", "responsible_owner_id", "snapshot_root", "manifest_path", "manifest_sha256", "cutoff_at", "output_path", "consumption_receipt_path", "human_reviewer_id", "retention_acknowledged", "allowed", "forbidden"],
  "properties": {
    "contract_id": {"const": "PILOT-LUMBIO-SHADOW-0.2-R1"},
    "contract_version": {"const": "0.1.0-draft"},
    "approval_status": {"const": "APPROVED_ONE_RUN"},
    "approval_id": {"type": "string", "pattern": "^[a-z0-9][a-z0-9._-]{2,127}$"},
    "run_id": {"type": "string", "pattern": "^[a-z0-9][a-z0-9._-]{2,127}$"},
    "approved_at": {"type": "string", "format": "date-time"},
    "expires_at": {"type": "string", "format": "date-time"},
    "pack_baseline_commit": {"const": "1b7d60590fe5e6d83f63032fd64fb647a2191296"},
    "execution_commit": {"type": "string", "pattern": "^[a-f0-9]{40}$"},
    "pilot_contract_sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "execution_bundle_sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "object_id": {"type": "string", "pattern": "^[a-z0-9][a-z0-9._-]{2,127}$"},
    "object_label": {"type": "string", "minLength": 1},
    "legal_entity_id": {"type": "string", "minLength": 1},
    "organization_id": {"const": "lumbio"},
    "scope_id": {"const": "lumbio"},
    "responsible_owner_id": {"type": "string", "minLength": 1},
    "snapshot_root": {"type": "string", "pattern": "^/[^\\u0000]+$"},
    "manifest_path": {"type": "string", "pattern": "^/[^\\u0000]+$"},
    "manifest_sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "cutoff_at": {"type": "string", "format": "date-time"},
    "output_path": {"type": "string", "pattern": "^/[^\\u0000]+$"},
    "consumption_receipt_path": {"type": "string", "pattern": "^/[^\\u0000]+$"},
    "human_reviewer_id": {"type": "string", "minLength": 1},
    "retention_acknowledged": {"const": true},
    "allowed": {"const": ["one_manual_read_only_shadow_run", "one_local_private_draft", "one_metadata_only_audit", "one_consumption_receipt", "one_human_review", "required_cleanup"]},
    "forbidden": {"const": ["second_run", "different_object_or_manifest", "live_connectors", "network", "schedule", "source_or_external_system_writes", "gbrain_write", "external_actions", "push_pr_merge_deploy_release_publish"]}
  },
  "additionalProperties": false
}
